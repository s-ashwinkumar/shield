# Handover — 2026-07-09

## What we're doing right now
Brainstorming how to **port the dev-workflow "playbook" into the rhythms repo** so any tool
(Cursor / Codex / Claude) can use it when starting a ticket. Using `superpowers:brainstorming`.

**DECIDED (2026-07-09): opt-in soft launch.** Playbook goes into rhythms as `docs/workflow.md`,
but **NO pointer in AGENTS.md yet** and no per-tool entry points. It's an *option*: rdev scripts
drive it for Ashwin; teammates invoke it explicitly ("follow docs/workflow.md"). Make it the
standard (add AGENTS.md pointer) only after a few people adopt it.

## Core architecture (established, don't re-derive)
- **What's used today = the `coordinator` agent** (`agents/coordinator.md`): holds the 5-stage
  pipeline (Plan → Build → Review → PR → PR-comments) + gates + `.claude/rdev/state.json`.
- Driven by the **live tmux `rt*` scripts**: `rtstream`→coordinator (Plan, interactive,
  `superpowers:brainstorming`); `rtbuild`→coordinator dispatches **builder** subagent; `rtforward`→
  coordinator runs Stage 3 review via **`codex:codex-rescue`** (cross-model) + builder fixes →
  Stage 4 PR → Stage 5 PR comments (`/fix-pr`).
- Subagents: `builder.md`, `reviewer.md`, `codex:codex-rescue`. Layers: **coordinator = conductor
  (flow/gates)**, **builder/reviewer = workers (the doing)**, **skills = procedures**.
- `r*` (non-t) = herdr port built alongside. **`package/` = IGNORE** (abandoned distribution attempt).
- **`/start`,`/plan`,`/build`,`/ship` slash skills = NOT used in the live flow** (they're the
  package/ interface). Ignore them.

## The portability principle (in memory: workflow-portability-two-layers)
- **Playbook** (steps/gates/DoD/testing-tiers/Railway-QA) = tool-neutral → **rhythms repo**
  (tools auto-read instructions from the repo they work in; `AGENTS.md`).
- **Harness/driver** (coordinator agent, `rdev` commands, `state.json`, subagent dispatch) =
  tool-specific → stays in rdev. The coordinator AGENT does NOT port; the *playbook it follows* does.
- Likely target: `rhythms/docs/workflow.md` (neutral playbook, single source of truth) + short
  pointer in `rhythms/AGENTS.md`; Claude coordinator / Cursor rule / Codex AGENTS.md all *reference* it.

## Decisions locked this session
- **QA → Railway previews** (`https://webui-rhythms-pr-<PR>.up.railway.app/`; team-documented in
  rhythms/AGENTS.md "PR Preview"). Local promote/lease **PARKED**: `rpromote`/`runpromote`/`rlocal`
  have PARKED banners; coordinator Stage 2 + `parallel-testing` skill + builder updated to Railway.
- **Per-worktree DB isolation KEPT** for Tier 1/2 local tests: `RDEV_DB_SUFFIX` + `bin/rmlai`
  (+ railsapi `database.yml`, mlai `.dev.env`).
- **context-mode plugin REMOVED** (was a 3rd-party plugin injecting routing rules into AGENTS.md/
  CLAUDE.md; uninstalled + orphaned `~/CLAUDE.md` and `rdev/CLAUDE.md` deleted).
- **rhythms/AGENTS.md audit:** committed file is fairly clean/lean; only pollution was the
  context-mode block (user reverted). Minor future trims noted (mlai-specific bullet, big mermaid).

## Branch state
- **`rdev/parallel-dev-infra`** (worktree `~/code/rhythms-rdev-infra`) holds 3 rhythms changes:
  `.cursorignore`, `railsapi/config/database.yml`, `mlai/src/config/.dev.env`. **STALE** (forked from
  an old origin/main), **unpushed**. Decision: **keep it, commit upcoming rhythms changes there**,
  **freshen with `git merge origin/main` before writing**, **NO PR yet**.
- rdev repo: on `main`, clean; all this session's harness edits committed.

## Next step
User answers (1) vs (2) above → then design the playbook (phase-by-phase: Plan → Build → Review →
Ship), comparing today's coordinator stages against `docs/refs/transcript-lessons.md` (L8 transcript
analysis; full transcript at `~/Downloads/tactiq-free-transcript-iQyg-KypKAA.txt`) → write spec →
writing-plans.

## Key files
`agents/coordinator.md` · `agents/builder.md` · `agents/reviewer.md` · `bin/rt*` ·
`skills/parallel-testing.md` · `docs/refs/transcript-lessons.md` · `~/code/rhythms/AGENTS.md`
