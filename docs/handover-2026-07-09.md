# Handover — updated 2026-07-12

## What happened (2026-07-09/10)

The portable playbook is BUILT and iterated. MOVED 2026-07-12: it now lives in **rdev at `playbook/`**
(workflow.md + workflow/0-setup.md … 5-ship.md); rtstream copies it into each worktree's gitignored
`.claude/rdev/playbook/`; coordinator v2 reads it there. The rhythms branch `rdev/parallel-dev-infra` and its worktree are GONE (2026-07-12):
the 3 infra changes were cherry-picked onto `ashwin/dev-skills-updates` (fresh atop origin/main, unpushed). Spec: `docs/plans/portable-playbook-spec.md`.

### Playbook content (all user-approved)
- Flow: 0 Setup → 1 Plan (🧑 gate) → 2 Build → 3 Review → 4 QA → 5 Ship. Single mermaid diagram = canonical.
- **THE RULE**: any new code → fresh-eyes review again; functionality-affecting → re-QA too; ~3 passes no convergence → escalate to human.
- Build is the ONLY stage that writes code; review findings + QA issues loop back as batches (QA: whole rounds, never fix-by-fix; each QA bug fix carries a regression test; scope discipline).
- Stage 0 triage: ticket exists/missing (missing → stop, get one created) · spec quality (well-spec'd → skip questions) · size (lightweight/design).
- Plan gate = state condition, 4 satisfaction paths: live / pre-approved / async / **spec-as-approval** (well-spec'd ticket + post derived plan to ticket + proceed; one material guess → downgrade to async).
- QA: every change, method menu per touched interface (browser/HTTP/MCP-client/jobs/CLI); Railway preview, never local unless plan says so; persistent-profile browser login; tests by builder judgment (NOT mandated TDD); remote agents verify tests via CI on early draft PR.
- Review: fresh eyes = different model FAMILY (codex for rdev; Cursor switches model); receiving-code-review discipline named at consumption points.
- workflow.md has "What a harness must provide" (5 ops) + "Precedence" section (5 named skill collisions: TDD mandate, finishing-a-development-branch handoff, per-task review ≠ stage 3, plan artifact paths, planning ceremony vs triage).

### rdev state — IMPORTANT SPLIT
- **Prefix split (2026-07-12): `rstream` (r*) = NEW playbook system → `agents/coordinator.md` (v2, promoted). `rtstream` (rt*) = OLD system → `agents/coordinator-v1.md` (rtstream launches coordinator-v1 by default; override via RDEV_COORDINATOR).** rstream copies the playbook into worktrees (creation + resume); rtstream does not.
- **`agents-next/coordinator.md` = v2** (pure bindings, executes `.claude/rdev/playbook/`, lavish, codex-companion fix, Supercut-demo/dev1 closing flags). Promote to agents/ after validation on a real ticket.
- Skills updated (live, compatible with stable coordinator): `qa` (Railway-default URL, persistent-profile auth `~/.rdev/browser-profile`, no ask-user-login), `parallel-testing` (QA for every change), `fix-pr-comments` (receiving-code-review discipline), `lavish` vendored (rdev-only, NOT rhythms), `review` skill DELETED (stale; rhythms .agents/skills/review supersedes).
- Cross-model review mechanism: codex plugin's `codex-companion.mjs adversarial-review` via Bash (NOT codex:codex-rescue — it refuses reviews).
- rhythms main branch protection verified: 1 review + 5 checks + enforce_admins (deterministic backstop).

## All stage decisions RESOLVED (2026-07-12)
- QA browser: chrome-devtools MCP ATTACH mode to shared QA Chrome (bin/rqa-browser, :9222, profile ~/.rdev/qa-chrome); /qa fallback chain devtools > playwright > human checklist. Global playwright MCP entry fixed (profile flag added).
- Railway previews are FULL-STACK (webui+railsapi+mlai+mcpservers); MCP QA = client config pointed at preview.
- Evidence: "What was tested" + text outputs in PR body; images/video uploaded to Linear ticket QA comment; NEVER committed to repo.
- Stage 5 closing: request human review on GitHub only (no Linear comment — automations own ticket lifecycle); agent never merges. After dev-verified: Supercut demo on preview (human, agent tees up via demo_required state flag) → ready for dev1 (dev1_ready flag).

## Open threads (next session)
1. USER ACTIONS pending: `claude mcp add -s user chrome-devtools -- npx -y chrome-devtools-mcp@latest --browserUrl http://127.0.0.1:9222 --experimentalPageIdRouting` · run `rqa-browser` + one-time Google sign-in (dedicated QA account) · optionally remove playwright MCP entries later.
2. **Validate v2**: run a real ticket through agents-next/coordinator.md (promote to agents/ when happy).
3. The 3 rhythms infra changes now sit on `ashwin/dev-skills-updates` (no worktree, unpushed, no PR — user call on when).
4. Remote-agent lens TODOs parked: browser auth on cloud runners (storage-state/test account), stage-3 model switching.

## Key facts
- Playbook = the program; harnesses provide 5 ops (state, implementer, fresh-eyes reviewer, test runner, QA executor, human channel — 6 listed in doc).
- Opt-in rollout: NO AGENTS.md pointer yet (memory: playbook-opt-in-rollout).
- Three-lens check for every step: rdev / local Cursor-Codex / remote agents (memory: three-lens-playbook-check).
- rhythms `.agents/skills/` vendors superpowers + rhythms-specific skills; audit done, conflicts handled via precedence section.
- rdev repo also has uncommitted `CLAUDE.md` deletion (pre-existing; user's call).
- The 3 infra changes (.cursorignore, database.yml, mlai .dev.env) live on `ashwin/dev-skills-updates` atop origin/main (2026-07-12).

## Key files
`playbook/workflow.md` + `playbook/workflow/*.md` (in rdev) · `agents-next/coordinator.md` (v2) · `bin/rqa-browser` · `bin/rtstream` (playbook copy step) ·
`agents/coordinator.md` (stable) · `skills/qa/*` · `docs/plans/portable-playbook-spec.md` ·
`docs/refs/transcript-lessons.md`
